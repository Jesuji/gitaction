name: CI
on: push
jobs:
  calculate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: gitaction
        uses: action.yml
        with:
          x: 12
          y: 3
